#include "main.h"

void doubleStrafePID();
/*
float singleAxisPID(float initialValue, float currentValue, float desiredValue, kP, kI, kD, cD, cI){
    float cP = currentValue / (desiredValue - initialValue)
    float value = cP * 1 + kP * kD + cD * cI;
    return(value);
}
*/
